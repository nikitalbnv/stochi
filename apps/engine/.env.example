# Environment
PORT=8080
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Internal service auth (generate with: openssl rand -base64 32)
# Must match ENGINE_INTERNAL_KEY in the web app
INTERNAL_KEY=

# Observability
SERVICE_NAME=stochi-engine
LOG_LEVEL=info
LOG_FORMAT=json
